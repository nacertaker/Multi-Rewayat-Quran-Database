# 📖 قاعدة بيانات القرآن الكريم متعدد الروايات

قاعدة بيانات SQLite شاملة للقرآن الكريم تدعم **6 روايات** مع التفسير والترجمة والتلاوات الصوتية.

## ✨ المميزات

- ✅ **6 روايات قرآنية** مع الخطوط العثمانية الأصلية
- ✅ **604 صفحة** لكل رواية (مطابقة للمصحف)
- ✅ **التفسير الميسر** (لرواية حفص)
- ✅ **الترجمة الإنجليزية** (Saheeh International)
- ✅ **24 قارئ** مع توقيتات دقيقة لكل آية
- ✅ **فهارس شاملة** (سور، أجزاء، أحزاب، أرباع)

## 📊 محتويات قاعدة البيانات

| الجدول | الوصف | العدد |
|--------|-------|-------|
| `riwayat` | معلومات الروايات | 6 |
| `surahs` | فهرس السور | 684 (114 × 6) |
| `ayat` | الآيات | ~37,000 |
| `lines` | الأسطر (للعرض) | ~54,000 |
| `juzs` | الأجزاء | 180 |
| `ahzab` | الأحزاب | 360 |
| `quarters` | الأرباع | 1,440 |
| `tafseer` | التفسير | 6,236 |
| `translations` | الترجمات | 6,236 |
| `reciters` | القراء | 33 |
| `ayah_timings` | توقيتات الآيات | 183,534 |

## 🚀 البدء السريع

### استخدام قاعدة البيانات مباشرة

```python
import sqlite3

# الاتصال بقاعدة البيانات
conn = sqlite3.connect('quran_database.db')
conn.row_factory = sqlite3.Row
cursor = conn.cursor()

# جلب آيات سورة الفاتحة
cursor.execute('''
    SELECT * FROM ayat 
    WHERE sura_no = 1 AND riwayah_key = 'hafs'
    ORDER BY aya_no
''')
for row in cursor.fetchall():
    print(f"آية {row['aya_no']}: {row['text']}")

conn.close()
```

### تشغيل الديمو

```bash
# تثبيت المتطلبات
pip install flask flask-cors

# تشغيل الخادم
python demo/api_server.py

# افتح المتصفح على
# http://localhost:5000
```

## 📁 هيكل المشروع

```
Quran_Multi-Riwayat_Database/
│
├── quran_database.db              # ⭐ قاعدة البيانات الرئيسية
│
├── data/                          # البيانات المُعالجة
│   ├── quran_index/               # فهارس JSON
│   └── audio_recitations/         # بيانات التلاوات
│
├── demo/                          # مثال للاستخدام
│   ├── api_server.py              # خادم API
│   ├── index.html                 # الواجهة
│   ├── app.js                     # منطق التطبيق
│   ├── style.css                  # التنسيقات
│   └── fonts/                     # الخطوط العثمانية
│
├── scripts/                       # سكريبتات البناء
│   ├── build_database.py          # بناء قاعدة البيانات
│   ├── final_smart_extract.py     # استخراج الأسطر
│   ├── create_quran_index.py      # إنشاء الفهارس
│   ├── create_ahzab_index.py      # إنشاء فهرس الأحزاب
│   └── collect_*.py               # جمع التلاوات
│
├── fonts/                         # الخطوط العثمانية (8 خطوط)
├── english_saheeh.csv             # الترجمة الإنجليزية
└── surahs_name.json               # أسماء السور
```

## 📋 API Reference

### الروايات

```
GET /api/riwayat                   # قائمة الروايات
GET /api/riwayat/<key>             # رواية محددة
```

### السور والآيات

```
GET /api/surahs?riwayah=hafs       # قائمة السور
GET /api/ayat?sura=1&riwayah=hafs  # آيات سورة
GET /api/ayat/<sura>/<aya>         # آية محددة
```

### الصفحات (للعرض المطابق للمصحف)

```
GET /api/page/<num>?riwayah=hafs   # صفحة كاملة مع الأسطر
GET /api/lines?page=1&riwayah=hafs # أسطر صفحة
```

### الفهارس

```
GET /api/juzs?riwayah=hafs         # فهرس الأجزاء
GET /api/ahzab?riwayah=hafs        # فهرس الأحزاب
GET /api/quarters?riwayah=hafs     # فهرس الأرباع
```

### التفسير والترجمة

```
GET /api/tafseer/<sura>/<aya>      # تفسير آية
GET /api/tafseer/<sura>            # تفسير سورة
GET /api/translation/<sura>/<aya>  # ترجمة آية
```

### التلاوات الصوتية

```
GET /api/reciters?riwayah=hafs     # قائمة القراء
GET /api/timings/<reciter>/<sura>  # توقيتات سورة
```

### البحث

```
GET /api/search?q=الرحمن&riwayah=hafs  # البحث في القرآن
```

## 🎧 الروايات والقراء المتاحين

| الرواية | المفتاح | القراء |
|---------|---------|--------|
| حفص عن عاصم | `hafs` | 18 قارئ |
| ورش عن نافع | `warsh` | 2 قارئ |
| قالون عن نافع | `qaloun` | 2 قارئ |
| الدوري عن أبي عمرو | `douri` | 1 قارئ |
| شعبة عن عاصم | `shuba` | 1 قارئ |
| السوسي عن أبي عمرو | `sousi` | - |

## 🔧 إعادة بناء قاعدة البيانات

```bash
# تثبيت المتطلبات
pip install -r scripts/requirements.txt

# استخراج الأسطر من ملفات Word (اختياري)
python scripts/final_smart_extract.py

# إنشاء الفهارس
python scripts/create_quran_index.py
python scripts/create_ahzab_index.py

# جمع التلاوات (اختياري)
python scripts/collect_all_reciters_with_timings.py

# بناء قاعدة البيانات
python scripts/build_database.py
```

## 📚 المصادر

- **بيانات الروايات**: [مجمع الملك فهد لطباعة المصحف الشريف](https://qurancomplex.gov.sa/)
- **التفسير الميسر**: مجمع الملك فهد
- **الترجمة الإنجليزية**: [QuranEnc.com](https://quranenc.com) - Saheeh International
- **التلاوات الصوتية**: [mp3quran.net](https://mp3quran.net)

## 📄 الترخيص

البيانات القرآنية مقدمة من مجمع الملك فهد لطباعة المصحف الشريف.
يُسمح باستخدامها في تطبيقات القرآن الكريم غير التجارية.

---

**تاريخ الإنشاء**: نوفمبر 2025

# مكتبات Python المطلوبة لاستخراج البيانات من ملفات Word

# لقراءة ملفات Word (.docx)
python-docx>=0.8.11

# لمعالجة XML (تستخدمها python-docx)
lxml>=4.9.0

# لمعالجة JSON
# (مدمجة في Python)

# لقاعدة البيانات SQLite
# (مدمجة في Python)


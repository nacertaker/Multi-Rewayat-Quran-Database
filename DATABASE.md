# 🗃️ قاعدة البيانات

## 📥 الحصول على قاعدة البيانات

قاعدة البيانات (`quran_database.db`) **غير موجودة** في المستودع لأن حجمها كبير (37 MB).

### خيارات الحصول على قاعدة البيانات:

#### 1. بناء قاعدة البيانات محلياً

```bash
# تثبيت المتطلبات
pip install -r scripts/requirements.txt

# ملاحظة: تحتاج البيانات الأصلية لإعادة البناء
# راجع scripts/build_database.py للتفاصيل
```

#### 2. تحميل من Release (قريباً)

سنقوم بإنشاء Release يحتوي على قاعدة البيانات.

#### 3. استخدام البيانات الموجودة

المشروع يحتوي على:
- ✅ **فهارس JSON** في `data/quran_index/`
- ✅ **بيانات التلاوات** في `data/audio_recitations/`
- ✅ **أسطر الروايات** في `scripts/output/`

يمكنك استخدام هذه البيانات مباشرة أو بناء قاعدة البيانات منها.

## 📊 محتويات قاعدة البيانات

- **6 روايات** قرآنية
- **~37,000 آية**
- **~54,000 سطر** (للعرض المطابق للمصحف)
- **6,236 تفسير**
- **6,236 ترجمة** إنجليزية
- **28 قارئ** مع **183,534 توقيت**

## 🔧 إعادة بناء قاعدة البيانات

إذا كنت تريد إعادة بناء قاعدة البيانات، ستحتاج:

1. البيانات الأصلية من مجمع الملك فهد
2. ملفات Word للروايات
3. ملف التفسير الميسر
4. ملف الترجمة الإنجليزية

راجع `scripts/build_database.py` للتفاصيل.

---

**ملاحظة**: الديمو في `demo/` يعمل مع قاعدة البيانات. تأكد من وجودها قبل التشغيل.


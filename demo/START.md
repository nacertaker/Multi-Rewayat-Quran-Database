# 🚀 تشغيل الديمو

## الخطوات:

1. **تأكد من وجود قاعدة البيانات:**
   ```bash
   # يجب أن يكون الملف موجوداً في المجلد الرئيسي
   ../quran_database.db
   ```

2. **تثبيت المتطلبات:**
   ```bash
   pip install flask flask-cors
   ```

3. **تشغيل الخادم:**
   ```bash
   python api_server.py
   ```

4. **فتح المتصفح:**
   ```
   http://localhost:5000
   ```

## 🔍 حل المشاكل:

### إذا ظهر خطأ "قاعدة البيانات غير موجودة":
- تأكد من أن `quran_database.db` موجود في المجلد الرئيسي
- المسار الصحيح: `../quran_database.db` من مجلد `demo/`

### إذا لم تظهر الخطوط:
- تأكد من وجود الخطوط في `demo/fonts/`
- يجب أن تكون 7 ملفات `.ttf`

### إذا لم يعمل API:
- تأكد من تثبيت `flask` و `flask-cors`
- تحقق من أن المنفذ 5000 غير مستخدم

---

**ملاحظة**: الديمو يعمل مع قاعدة البيانات الجاهزة فقط. لا يحتاج للملفات المصدرية المحذوفة.


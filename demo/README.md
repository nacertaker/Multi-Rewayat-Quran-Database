# 🖥️ مثال استخدام قاعدة بيانات القرآن الكريم

هذا المجلد يحتوي على **مثال تطبيقي** لكيفية استخدام قاعدة البيانات.

## 🚀 التشغيل

### 1. التحقق من قاعدة البيانات

```bash
# من المجلد الرئيسي
python demo/test_db.py
```

يجب أن ترى:
```
✅ قاعدة البيانات موجودة
✅ عدد الروايات: 6
✅ عدد الآيات: 37334
...
```

### 2. تثبيت المتطلبات

```bash
pip install flask flask-cors
```

### 3. تشغيل الخادم

```bash
# من مجلد demo
cd demo
python api_server.py
```

### 4. فتح المتصفح

```
http://localhost:5000
```

## ⚠️ ملاحظات مهمة

- **قاعدة البيانات** يجب أن تكون في المجلد الرئيسي: `../quran_database.db`
- **الخطوط** موجودة في `demo/fonts/` (7 ملفات)
- **لا حاجة** للملفات المصدرية المحذوفة - قاعدة البيانات جاهزة

## 📁 الملفات

| الملف | الوصف |
|-------|-------|
| `api_server.py` | خادم Flask API |
| `index.html` | واجهة المستخدم |
| `app.js` | منطق JavaScript |
| `style.css` | التنسيقات CSS |
| `fonts/` | الخطوط العثمانية |

## ✨ المميزات

- 📖 عرض 6 روايات مختلفة
- 📑 فهارس السور والأجزاء والأحزاب
- 📝 التفسير الميسر
- 🌍 الترجمة الإنجليزية
- 🎧 التلاوات الصوتية مع التوقيتات
- 🔍 البحث في القرآن

## 🎮 الاستخدام

1. **اختيار الرواية**: من القائمة العلوية
2. **التنقل**: أزرار السابق/التالي أو أسهم لوحة المفاتيح
3. **الفهارس**: أزرار 📖 📚 📑 في الشريط العلوي
4. **التفسير/الترجمة**: انقر على أي آية
5. **الاستماع**: اختر قارئ من الشريط السفلي
6. **تشغيل آية**: انقر مرتين على الآية

## 🔌 API Endpoints

جميع الـ endpoints متاحة على `/api/`:

```
/api/riwayat          - الروايات
/api/surahs           - السور
/api/page/<num>       - صفحة
/api/tafseer/<s>/<a>  - التفسير
/api/translation/<s>  - الترجمة
/api/reciters         - القراء
/api/search?q=...     - البحث
```

---

**ملاحظة**: هذا مجرد مثال للتوضيح. يمكنك بناء تطبيقك الخاص باستخدام قاعدة البيانات مباشرة.

